create table Organisateur
  (
  num integer,
  nom varchar(20),
  ville varchar(20),
  code_ps char(5),
  rue varchar(20),
  tel char(10),
  mail varchar(30),
  specialisation varchar(20),
  );

create table Location
  (
  num integer,
  nom varchar(20);
  ville varchar(20),
  code_ps char(5),
  rue varchar(30),
  tel char(10),
  mail varchar(30),
  );
